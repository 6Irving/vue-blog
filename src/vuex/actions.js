export const setOnlineUser = ({ dispatch }, user) => { dispatch('SET_ONLINEUSER', user) }

export const addUser = ({ dispatch }, user) => { dispatch('ADD_USER', user) }

export const addPost = ({ dispatch }, post) => { dispatch('ADD_POST', post) }